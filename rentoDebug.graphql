mutation rentoDebug {
  goto(url: "https://www.example.com", waitUntil: networkIdle) {
    status
  }
  currentUrl: evaluate(
    content: """
    document.location.href.includes('example') ? 
      'yes' 
    : 
      'no';
    """
  ) {
    value
  }
}